<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
      Cleartext is off everywhere by default (API 28+), which is what we want in
      production. The emulator reaches the developer's machine on 10.0.2.2 over
      plain HTTP, so exactly that host is exempted, and only in the debug
      manifest — the release build has no exemption at all and cannot be
      configured into one by accident.
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
