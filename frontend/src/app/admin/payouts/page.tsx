'use client';
import { WithdrawalsView } from '@/features/withdrawals/withdrawals-view';
export default function Page() {
  return <WithdrawalsView title="Payouts" subtitle="Money leaving the platform — approvals and the 48-hour SLA." />;
}
